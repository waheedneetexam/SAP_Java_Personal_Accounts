{
  "name": "SAP_Java_Personal_Accounts-cds",
  "version": "1.0.0",
  "description": "Digital Library Hub — SAP CAP app with HANA and SQLite support",
  "license": "ISC",
  "repository": "<Add your repository here>",
  "scripts": {
    "start": "cds-serve",
    "start:sqlite": "cds watch --in-memory",
    "start:hana": "cds watch --profile hybrid",
    "start:hana:prod": "cds watch --profile production,hybrid",
    "build:hana": "cds build --production"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^1.11.1",
    "@sap/cds-dk": "^9.5.0"
  },
  "dependencies": {
    "@cap-js/hana": "^1.7.0",
    "@sap/xssec": "^4.12.2"
  }
}
